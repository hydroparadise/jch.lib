#!/bin/bash

# Tested
# Ubuntu 25 Plucky Live

# perform update
sudo add-apt-repository universe -y
sudo apt-get update -y

# Git
sudo apt-get install git -y

# Maven
sudo apt-get install maven -y

# Zip
sudo apt-get install unzip -y


